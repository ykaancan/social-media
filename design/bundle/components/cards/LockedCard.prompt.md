Locked inbox card: hatched surface, dashed border, blurred bars sized from the real character count. Never fake the sender level, length or source.

```jsx
<LockedCard level="hint" hints={{ country: "Italy" }} length={164} time="3h" source="National Platform" onUnlock={...} labels={t.inbox} />
```
