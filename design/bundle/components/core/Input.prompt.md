Labelled text field; `multiline` for bios and the composer body.

```jsx
<Input label="One-line bio" value={bio} onChange={setBio} maxLength={80} placeholder="Where you're from, what you're into" />
<Input label="Email" type="email" error="That doesn't look like an email" />
```
