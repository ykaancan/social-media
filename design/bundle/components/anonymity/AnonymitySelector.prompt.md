Composer anonymity picker; remembers the last choice at the app level (pass it back in as `value`).

```jsx
<AnonymitySelector value={lvl} onChange={setLvl} hintFields={hf} onHintFieldsChange={setHf}
  me={{ name: "Şeyma", section: "ESN İzmir", country: "Türkiye" }} labels={t.composer} />
```
